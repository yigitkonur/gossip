# AGENTS.md

This file applies to `ts-legacy/src/cli/`.

## What this folder is
- This folder contains the old TypeScript CLI commands.
- It shows how the legacy system started Claude, Codex, and the daemon.

## When editing here
- Read this folder when you need to compare old user-facing behavior.
- Do not add new active features here.

## Keep these rules true
- Treat it as a historical workflow reference.
- Use it to understand parity gaps, not to drive new runtime changes directly.
