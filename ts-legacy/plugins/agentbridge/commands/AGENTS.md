# AGENTS.md

This file applies to `ts-legacy/plugins/agentbridge/commands/`.

## What this folder is
- This folder holds legacy command prompt files for the old plugin.
- These files explain what the old plugin asked Claude Code to do.

## When editing here
- Only update this folder if you are preserving or explaining legacy behavior.
- Do not use it as the source of truth for the current Go CLI.

## Keep these rules true
- Keep command text easy to read.
- Make it obvious when behavior is legacy-only.
