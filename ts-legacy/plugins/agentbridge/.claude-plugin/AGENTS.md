# AGENTS.md

This file applies to `ts-legacy/plugins/agentbridge/.claude-plugin/`.

## What this folder is
- This is legacy plugin metadata for the old TypeScript/Bun plugin.
- It describes the old plugin identity, not the current Go runtime.

## When editing here
- Only touch this folder if you are documenting or comparing the legacy plugin.
- Prefer the current `plugins/agentbridge/.claude-plugin/` folder for active work.

## Keep these rules true
- Treat everything here as reference-only.
- Keep metadata simple and historically accurate.
