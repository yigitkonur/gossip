# AGENTS.md

This file applies to `ts-legacy/plugins/agentbridge/server/`.

## What this folder is
- This folder contains built JavaScript bundles from the old plugin.
- These files were generated outputs, not the best source for understanding intent.

## When editing here
- Avoid changing these files unless a human explicitly asks for legacy bundle work.
- Prefer `ts-legacy/src/` when you need the real logic.

## Keep these rules true
- Treat these files as packaged output.
- Do not use them as the first stop for behavior analysis.
